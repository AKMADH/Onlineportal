@model IEnumerable<OnlineEXAM.Models.maincateogry>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MainCateogryName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CreatedDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MainCateogryName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CreatedDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MainCateogryID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MainCateogryID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MainCateogryID })
        </td>
    </tr>
}

</table>
